{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build with Makefile",
            "type": "shell",
            "command": "make",
            "args": [
                "all"
            ],
            "options": {
                "cwd": "${fileDirname}/../../.."
            },
            "group": "build",
            "problemMatcher": [
                "$gcc"
            ],
            "detail": "通过 Makefile 编译项目"
        }
    ]
}